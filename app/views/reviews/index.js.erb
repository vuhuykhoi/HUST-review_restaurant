$('#list_search_reviews').append('<%=j render(partial: "reviews/search_review", collection: @reviews, as: :r )%>');
<% if @reviews.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @reviews %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();

<% end %>